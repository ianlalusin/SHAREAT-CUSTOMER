rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Prototype Access: Allow all reads and writes to facilitate rapid testing
    // without Firebase Authentication dependencies.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}